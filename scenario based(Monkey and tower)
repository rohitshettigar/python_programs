def cal(x, a, b):
    height = 0
    time = 0
    while True:
        time += 1
        if time % 2 == 1:  
            height += a
            if height >= x:
                return time
        else:  
            height -= b
print(cal(25,9,3))
